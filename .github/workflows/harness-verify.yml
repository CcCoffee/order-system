name: Harness Verification

on:
  pull_request:
  push:
    branches:
      - main

jobs:

  verify:
    runs-on: ubuntu-latest

    steps:

      - name: Checkout
        uses: actions/checkout@v4

      - name: Verify Harness
        run: ./scripts/verify.sh
